Use the orchestrator agent. Read `docs/project-requirements.md` and `docs/project-state.md`. Generate a prioritized list of tasks and write them to the `## Active TODOs` section of `docs/project-state.md`.

**Do NOT implement anything. Do NOT dispatch the implementer or researcher. Planning only.**

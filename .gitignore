# Claude Code
.claude/settings.local.json

# Transient session runtime state (per-container scratch, never a deliverable)
.claude/tmp/

# OS
.DS_Store
Thumbs.db

# Plan scratch files (transient — the developer overwrites each cycle and
# deletes when done; not needed after the bundled commit, never pushed).
.claude/handoff/*-plan.md

# Obsidian per-user vault config (users open the vault themselves)
docs/.obsidian/

# Claude Code
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Session checkpoints (transient — regenerated on every /project:checkpoint)
docs/wiki/session-checkpoint.md

# Inter-agent handoff files (transient — written by tester, deleted by implementer)
.claude/handoff/

# Obsidian per-user vault config (users open the vault themselves)
docs/.obsidian/

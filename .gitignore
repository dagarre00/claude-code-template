# Claude Code
.claude/settings.local.json
.claude/tmp/

# OS
.DS_Store
Thumbs.db

# Plan and findings scratch (transient — overwritten each cycle and deleted
# when done; not needed after the bundled commit, never pushed). The record
# that survives is the commits they drove plus the log.md entry.
.claude/handoff/*-plan.md
.claude/handoff/*-findings.md

# Obsidian per-user vault config (users open the vault themselves)
docs/.obsidian/
